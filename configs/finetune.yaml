project:
  name: arabic-script-turkic-ocr
  run_name: finetune

training:
  seed: 42
  epochs: 10
  batch_size: 16
  lr: 0.0001

data:
  train_list: data/splits/v1/train.txt
  val_list: data/splits/v1/val.txt
  test_list: data/splits/v1/test.txt
