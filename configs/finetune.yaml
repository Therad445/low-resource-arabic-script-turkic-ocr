project:
  name: arabic-script-turkic-ocr
  run_name: finetune

training:
  seed: 42
  epochs: 10
  batch_size: 16
  lr: 0.0001

data:
  train_list: data/splits/v1/train_docs.txt
  val_list: data/splits/v1/dev_docs.txt
  test_list: data/splits/v1/test_docs.txt